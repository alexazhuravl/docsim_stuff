#0
"""
df = df.replace('nan', np.nan).dropna() replace string 'nan' to null value and then drop rows
"""

#1
"""
list = [1, 2, 3]
cnt = 0
for y in range(len(list)):
    arr = []
    for x in range(len(list)):
        arr += [list[cnt] * list[x]]
    cnt = cnt+1
    test[y+1] = arr
test -- fill matrix with values (multiply)
"""